# OUT LAN

Two-player home-LAN branch for Skyrim Together Reborn.

This directory will contain deployment and launch helpers. Keep the git checkout in a `_OUT_STR` subdirectory of the Skyrim installation rather than turning the game directory itself into a repository.

Initial target: one host, one guest, same Skyrim build, same mod/load-order state, host-authoritative world state.
